package api.controller.tree;

import api.model.tree.Personne;
import api.service.tree.PersonneService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.List;

/*
@RestController
@RequestMapping("/api")
public class PersonneController {
    @Autowired
    private PersonneService personneService;

    @GetMapping("/personnes/tree/{treeId}")
    public List<Personne> getPersonnesByTreeId(@PathVariable Long treeId) {
        return personneService.findByTreeId(treeId);
    }

    // Autres endpoints CRUD pour Personne
    // ...
}*/

// Hello
